# Python 3.13 is the recommended runtime for this challenge.
# dbt-core 1.12.4 officially supports Python 3.14, while dbt-duckdb 1.11.0
# currently publishes Python 3.13 classifiers; Python 3.13 is therefore the
# conservative cross-machine choice for this local DuckDB stack.
dbt-core==1.12.4
dbt-duckdb==1.11.0

# AI layer
anthropic==1.4.0

# Python analysis / notebook layer
pandas==3.0.5
jupyter==1.1.1
pyyaml==6.0.3

# Compatibility pin required by the dbt semantic-interfaces dependency tree.
importlib-metadata==8.7.0

# MCP bonus layer
mcp[cli]==2.2.0
